export enum SidebarRoutes {
    dashboard = "/dashboard",
    users = "/users",
    products = "/products",
    authentication = "/authentication",
    typography = "/typography",
    icons = "/icons",
    support = "/"
}
